draw_self();
draw_set_font(__fhInputFntSml);
draw_set_halign(fa_left);
draw_text(x+32, y, "Hold Action #0(Down Status) to shoot.");
draw_text(x+32, y+32, "It has a small cooldown so it doesn't");
draw_text(x+32, y+64, "shoot a constant stream of bullets.");