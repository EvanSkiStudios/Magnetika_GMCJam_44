draw_self();
draw_set_font(__fhInputFntSml);
draw_set_halign(fa_right);
draw_text(x+100, y+32, "Use Action #2(Release Status) to shoot this one.");
draw_text(x+100, y+64, "Note that it only shoots when the input is released.");